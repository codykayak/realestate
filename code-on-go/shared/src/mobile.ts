/**
 * Metro / Expo entry — extension-less imports (Metro cannot resolve .js → .ts).
 * Backend uses compiled dist/index.js instead (see shared/package.json exports).
 */
export * from './types';
export * from './constants';
export * from './cursor';
